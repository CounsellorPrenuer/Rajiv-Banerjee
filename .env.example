# Copy to .env.local for local-only use. Never commit real values.

# Sanity (seed + studio deploy — run locally only)
SANITY_EDITOR_TOKEN=
SANITY_DEPLOY_TOKEN=

# Cloudflare Worker secrets are set via: cd worker && npx wrangler secret put RAZORPAY_KEY_ID
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
# RAZORPAY_WEBHOOK_SECRET=
